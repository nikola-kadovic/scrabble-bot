#include "scrabble/letter.hpp"

// All definitions are constexpr/inline in the header.
// This translation unit exists so the static library has a letter.cpp object.

namespace scrabble {
// (nothing to define here)
}  // namespace scrabble
